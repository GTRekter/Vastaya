{{- if and .Values.mcp.enabled .Values.mcp.service }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "vastaya.mcp.name" . }}
  labels:
    {{- include "vastaya.labels" . | nindent 4 }}
    app.kubernetes.io/component: mcp
spec:
  type: {{ .Values.mcp.service.type }}
  ports:
    - port: {{ .Values.mcp.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "vastaya.mcp.selectorLabels" . | nindent 4 }}
{{- end }}
