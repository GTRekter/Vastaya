{{- if and .Values.mcp.enabled .Values.mcp.googleApiKey }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "vastaya.mcp.secretName" . }}
  labels:
    {{- include "vastaya.labels" . | nindent 4 }}
    app.kubernetes.io/component: mcp
type: Opaque
stringData:
  google-api-key: {{ .Values.mcp.googleApiKey | quote }}
{{- end }}
